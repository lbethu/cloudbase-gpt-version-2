node_modules
.next
out
.DS_Store
*.log
.env*
!.env.example
# Intermediate rename artifacts from setting up .env.local — never commit these,
# they can contain real credentials in plain text.
env.local
env.local.txt
tests/fixtures/audit-tmp/
# Runtime state
var/
# Source documents are served only through the authorized file route; never from public/
public/source-documents/
